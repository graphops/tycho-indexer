# Rust build artifacts — by far the largest contributor to build context size
target/
**/target/

# Git history and local tooling
.git/
.claude/
.idea/
.vscode/

# Substreams workspace builds (separate toolchain, not needed for main images)
protocols/substreams/

# Local dev files
venv
*/venv
.env
docker-compose*.yml
*.swp
